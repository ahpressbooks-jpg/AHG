// Tailwind CSS v4 via PostCSS (redesign brief mandates Tailwind v4 with the
// design tokens as the single source of truth — see app/tsr-theme.css).
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
